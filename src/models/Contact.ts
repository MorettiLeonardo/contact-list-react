class Contact {
  id: number
  name: string
  email: string
  number: number

  constructor(id: number, name: string, email: string, number: number) {
    this.id = id
    this.name = name
    this.email = email
    this.number = number
  }
}

export default Contact
